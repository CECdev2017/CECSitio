<h1>Orden Recibida</h1>
<h3>Dear <%= @order.name %></h3>
<p>
  Gracias por inscribirte!!
</p>
<p>Detalle de Inscripcion:</p>
	<% @order.curso_items.each do |item| %>
	<ul>
		<li>
			<%= item.curso.titulo %>&times;<%= item.quantity %>
		</li>

		<li>
			<%= number_to_currency item.total_precio %>
		</li>
	</ul>		
<% end %>

<p>Total: <%= number_to_currency @order.total_precio %></p>
